#!/bin/sh

if [ ! -f "%{_sysconfdir}/sudoers.tmp" ]; then
  touch /etc/sudoers.tmp
  [ -f /etc/sudoers ] && cp /etc/sudoers /etc/sudoers.new
  echo "%users ALL=(ALL)NOPASSWD:/usr/bin/build" >> /etc/sudoers.new
  cp /etc/sudoers.new /etc/sudoers
  rm /etc/sudoers.tmp
fi
